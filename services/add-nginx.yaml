services:
  nginx:
    image: nginx:alpine
    container_name: sendthenoise-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ../nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - sendthenoise-net
    depends_on:
      - server
